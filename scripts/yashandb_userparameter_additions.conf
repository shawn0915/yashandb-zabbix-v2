# ---- 补充缺失的 UserParameter（2026-04-11 修复 Not Supported）----
# session 命名空间（Datadog/NewRelic）
UserParameter=yashandb.sessions.inactive,/bin/bash /etc/zabbix/scripts/yashandb/wrapper.sh --metric session.inactive
UserParameter=yashandb.sessions.background,/bin/bash /etc/zabbix/scripts/yashandb/wrapper.sh --metric session.background
UserParameter=yashandb.sessions.active_detail,/bin/bash /etc/zabbix/scripts/yashandb/wrapper.sh --metric session.active_detail

# redolog 命名空间（NewRelic）
UserParameter=yashandb.redo.waits,/bin/bash /etc/zabbix/scripts/yashandb/wrapper.sh --metric redolog.waits
UserParameter=yashandb.redo.switch_checkpoint,/bin/bash /etc/zabbix/scripts/yashandb/wrapper.sh --metric redolog.switch_checkpoint

# datafile 命名空间（NewRelic）
UserParameter=yashandb.tablespace.datafile_offline_count,/bin/bash /etc/zabbix/scripts/yashandb/wrapper.sh --metric datafile.offline_count

# archive_log 命名空间（NewRelic）
UserParameter=yashandb.archived_log.count,/bin/bash /etc/zabbix/scripts/yashandb/wrapper.sh --metric archive_log.count
UserParameter=yashandb.archived_log.first_time,/bin/bash /etc/zabbix/scripts/yashandb/wrapper.sh --metric archive_log.first_time

# enqueue 命名空间（NewRelic）
UserParameter=yashandb.lock.enqueue_locks,/bin/bash /etc/zabbix/scripts/yashandb/wrapper.sh --metric enqueue.locks
UserParameter=yashandb.lock.enqueue_requests,/bin/bash /etc/zabbix/scripts/yashandb/wrapper.sh --metric enqueue.requests

# 长事务（Nagios 命名空间已在上面，但 db.tablespace_remaining_days 等用的是 db.*）
# 注意：db.tablespace_remaining_days / db.datafile_max_usage_pct / db.dbms_stats_available 已在上面配置
